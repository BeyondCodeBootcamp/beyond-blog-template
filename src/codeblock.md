# Code Block Example

```js
// This is what a codeblock looks like. It should easily handle 80 lines of code
function isShort() {
  return false;
}

// and, if we're being generous................................ probably up to....................................... 120
function isLong() {
  return true;
}
```
